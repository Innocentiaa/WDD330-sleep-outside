Trello board: https://trello.com/b/W6k5ls9t/sleep-outside
Team members:
- Innocentia Chinweotito (IC)
- luthando ngombane (LN)
- Heaven Gabriel (HG)
- Santiago Irigoyen (SI)


import { resolve } from "path";
import { defineConfig } from "vite";

export default defineConfig({
  // root: "src/", // Keep this commented out

  build: {
    outDir: "dist",
    rollupOptions: {
      input: {
        main: resolve(__dirname, "src/index.html"),
        cart: resolve(__dirname, "src/cart/index.html"),
        checkout: resolve(__dirname, "src/checkout/index.html"),
        product: resolve(__dirname, "src/product_pages/index.html"),
        product_listing: resolve(__dirname, "src/product_listing/index.html"),
        wellness_hub: resolve(__dirname, "src/daily-wellness-hub/index.html"),
      },
    },
  },
});
